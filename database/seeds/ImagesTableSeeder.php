<?php

use App\Image;
use Illuminate\Database\Seeder;

class ImagesTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $faker = Faker::create();

        // $images = Image::pluck('id')->all();

        // foreach (range(1, 20) as $index)
        // {

        //     User::create([
        //         'image_id'        => str_random(1),
        //         'unique_name'     => $faker->name,
        //         'orginal_name'    => $faker->name,
        //         'image_path'      => $faker->name,
        //         'image_extension' => $faker->name,
        //     ]);
        // }
    }
}
